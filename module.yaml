notests *default:
  build:
    target: Vostok.ClusterClient.Transport.Native.sln
    configuration: NoTests
    tool:
      name: dotnet

  install:
    - Vostok.ClusterClient.Transport.Native/bin/Release/netstandard2.0/Vostok.ClusterClient.Transport.Native.dll

  deps:
    - vostok.devtools

full-build > notests:
  build:
    target: Vostok.ClusterClient.Transport.Native.sln
    configuration: Release